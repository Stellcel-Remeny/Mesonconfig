[BugFix]
OK- Fix bug with double <ESC><ESC> key press in Exit dialog
OK - Arrow keys do not switch between buttons in String or Exit dialog, currently only TAB works.
OK - Fix bug that happens when size < minimum And a window (like HelpScreen) is launched. It currently does not get hidden.
OK - If select KComment, program crashes.
OK - Scrollable functionality in Help window
- Help Window Markdown tables color do not change (basically currently invisible borders)
- Double check if an option (eg. bool2) depending on another option (eg. bool1):
    Example case: Make sure that if bool1 is False, and when bool2 depends on bool1, then bool2 must also be False.
OK - Single quotes Not stripped in Menu in Textual TUI [Strip quote When loading file, because quotes get added onto local.conf successively]
OK - Compare to-be-written local.conf with existing local.conf. If no match (exclude whitespace, alignment, order, and commands), THEN show confirm exit dialog
- When an option depending upon another option is shown, take its value from the defaults (Config.in) instead of local.conf, as when saving local.conf, we saved unset variables with False, 0, and "", even though user did not change it.
- "Terminal too small" message in TUI: All sub windows closes, returning to Menu. Do not close them.
[VIP] - KChoice Does NOT properly work (if Depends) (Problem could be with Depends)

[Quality improvements]
OK - Group by menu in generated local.conf and meson_options.txt (eg. "# --- Source configuration ---")
- Expand $() variables with variables captured from Python
OK - Window titles in sub windows
- Transparent & Darken bkgd of scrollbars everywhere
OK - Proper Help text (Overview And Option help text)
- Adjust helptext more
OK - Add Help button to String Dialog + Improve text visibility colour scheme
OK - Add Cancel button to Exit Dialog
OK - Do not show confirm exit if no change took place
OK - Rename existing local.conf to local.conf.old, on saving.
OK - Load existing local.conf
- Ask if you want to --configure or --reconfigure the project if <Yes> is selected in exit dialog
- Support lists[] and dicts[] in KConfig output file
OK - Support Choice variables + UI for them + KConfig updated output code
OK - Add code for <Load> and <Save> buttons in TUI
- Display [NEW!] in new options

[Features]
OK - Ability to generate meson_options.txt from KConfig file
- Add --add-config-prefix (which adds CONFIG_ to all the options)
- Support project configuration other than [project options] (eg. [binaries], [built-in options]). Can show even if KConfig is absent.
- Draggable windows
- Add Exit (X) button on windows
OK - Load mesonconfig.ini, which has settings (eg. window_color=, output_file=)
- Open searchbox by pressing '/' anywhere

[Eyecandy]
- Shadows for windows
- 3D Borders
- Theme presests

[Other]
- Code refactoring
- Performance improvements
- Security improvements